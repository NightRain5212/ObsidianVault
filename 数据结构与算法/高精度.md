
- 当需要`long long`无法存储的大整数的运算时，我们需要自己定义数据结构存放大整数进行运算，通常是开一个足够大的数组，倒序存储每一位数字

- 数据结构定义例子
```cpp
// 用数组存放每一位数字，从低位到高位，N为提前定义的数字长度
vector<int> nums(N);
```

### 高精度加法

- 先不考虑进位，对数组每一位相加得到结果后再统一进位。

### 高精度减法

- 模拟竖式运算

### 高精度乘以低精度

### 高精度乘以高精度

### 高精度除以低精度
